const membershipAuthenticate = (
  allowed_membership,
  current_membership,
  validation_items
) => {
  // Validate resources
  // Validate chains
  // Validate limits
  return (req, res, next) => {
    next();
  };
};

module.exports = { membershipAuthenticate };
